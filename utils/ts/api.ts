/** Demo stub — no backend API calls */
export const gameServerApi = {
  leaveGame: (_params: unknown, cb?: () => void) => cb?.(),
}

export const userContent = {}
export const wallet = {}
export const login = {}
export const VipBonus = {}
